## вообще можно сказать что наибольшее число по int и есть число с наибольшим колвом разрядов в
## двоичной системе исчисления. Но это показалось мне слишком легким.
n=int(input("Введите количество чисел n "))
c=[]
print("Теперь введите n чисел.")
for x in range(0,n):
    a = int(input())
    c.append(bin(a)[2:])
d = max(c, key=len)
print("Число с самым большим количеством разрядов в двоичной системе исчисления -",int(d, 2))
